# Secrets and environment variables
.env
*.env
.env.*
!.env.example

# Claude Code session data (planes, memoria del asistente del host) — no es del proyecto
.claude/

# Docker
.docker/

# OpenClaw / Agent data
.openclaw/
*.log
ingress-spool-*/

# Watchdog runtime state (el script scripts/naru-watchdog.sh SÍ se versiona; su log/estado no)
.naru-watchdog.last

# Gemini CLI OAuth credentials (free tier login) — NUNCA commitear
.gemini_cli/

# Memoria/continuidad del bot OpenClaw (MEMORY.md, memory/, HEARTBEAT.md, USER.md, TOOLS.md).
# Datos del agente, no código. Persistido vía volumen para sobrevivir recreates.
.openclaw_workspace/

# Godot specific
.godot/
*.import
.tmp/
# NOTA: export_presets.cfg SÍ se versiona — el CD lo necesita para exportar los juegos
# y la tarea SG-08 lo edita para publicar Signal. NO lo agregues a este ignore.

# OS generated files
.DS_Store
Thumbs.db

# .NET / C#
bin/
obj/
*.user
*.userprefs
*.sln.dotSettings.user
*.suo
*.cache
*.pdb
*.opendb
*.ide
*.orig
.vs/
.vscode/

# Node.js
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.npm
PERMISSIONS_REPORT.md
